eventEmitter.on('account_approval', async ({ data }) => {
  // event listener
  // send email
});
eventEmitter.on('company_registration', async ({ data }) => {
  // event listener
  // send email
});
